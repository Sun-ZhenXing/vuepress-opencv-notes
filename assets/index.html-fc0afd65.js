import{_ as p,M as t,p as c,q as l,R as n,N as s,V as o,t as e,a1 as d}from"./framework-8980b429.js";const i={},h=n("h1",{id:"_1-安装-opencv",tabindex:"-1"},[n("a",{class:"header-anchor",href:"#_1-安装-opencv","aria-hidden":"true"},"#"),e(" 1. 安装 OpenCV")],-1),r={class:"table-of-contents"},u=d(`<h2 id="_1-1-opencv-的-python-默认发行版" tabindex="-1"><a class="header-anchor" href="#_1-1-opencv-的-python-默认发行版" aria-hidden="true">#</a> 1.1 OpenCV 的 Python 默认发行版</h2><p>OpenCV 提供 PyPI 发行的 Python 包，使用 Python 的二进制扩展并且是预编译的。</p><div class="hint-container info"><p class="hint-container-title">下载预编译包</p><p>PyPI 目前只提供 CPU 版本的预编译包，如果你需要 CUDA 支持或其他架构支持的 OpenCV 可以查阅其他包管理工具或自行编译。</p></div><p>如果你的 Python 包管理器内没有安装 OpenCV，可以使用下面的命令直接安装：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pip <span class="token function">install</span> opencv-python
</code></pre></div><p>也可以指定版本：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pip <span class="token function">install</span> opencv-python<span class="token operator">==</span><span class="token number">4.7</span>.0.68
</code></pre></div><p>如果你使用 Anaconda，那么 OpenCV 已经默认安装，如果你想更新可以使用：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pip <span class="token function">install</span> <span class="token parameter variable">-U</span> opencv-python
<span class="token comment"># 或者</span>
conda update opencv-python
</code></pre></div><h2 id="_1-2-安装扩展包和无-gui-版本的-opencv" tabindex="-1"><a class="header-anchor" href="#_1-2-安装扩展包和无-gui-版本的-opencv" aria-hidden="true">#</a> 1.2 安装扩展包和无 GUI 版本的 OpenCV</h2><p>如果需要 OpenCV Contrib 模块中包含的算法，需要安装 <code>opencv-contrib-python</code>，安装命令如下：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pip <span class="token function">install</span> opencv-contrib-python
</code></pre></div><p>如果你在 <code>libGL</code> 支持不完备的系统（通常是无桌面的系统）上安装或使用 <code>opencv-python</code>，可能出现错误，可以安装无 GUI 支持的 OpenCV Headless 版本：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pip <span class="token function">install</span> opencv-python-headless
</code></pre></div><p>通常 Headless 版本用于服务器上使用。</p><p>同样，<code>opencv-contrib-python</code> 也提供 Headless 版本 <code>opencv-contrib-python-headless</code>：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pip <span class="token function">install</span> opencv-contrib-python-headless
</code></pre></div><div class="hint-container warning"><p class="hint-container-title">版本一致</p><p><code>opencv-python</code> 和 <code>opencv-python-headless</code> 不能一起安装，否则导入包时产生冲突，另一个包无法被加载。安装 Contrib 版本也要和默认的 OpenCV 版本一致，否则会出现不兼容的问题。</p></div><h2 id="_1-3-使用其他包管理器" tabindex="-1"><a class="header-anchor" href="#_1-3-使用其他包管理器" aria-hidden="true">#</a> 1.3 使用其他包管理器</h2><p>在 Debian/Ubuntu 上使用，可以使用系统的包管理器安装：</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt-get</span> <span class="token function">install</span> python3-opencv
</code></pre></div><p>在 Termux 上安装时，默认的 <code>pip</code> 安装命令可能失败，使用 <code>pkg</code> 包管理器安装</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pkg <span class="token function">install</span> opencv-python
</code></pre></div>`,23);function v(g,_){const a=t("router-link");return c(),l("div",null,[h,n("nav",r,[n("ul",null,[n("li",null,[s(a,{to:"#_1-1-opencv-的-python-默认发行版"},{default:o(()=>[e("1.1 OpenCV 的 Python 默认发行版")]),_:1})]),n("li",null,[s(a,{to:"#_1-2-安装扩展包和无-gui-版本的-opencv"},{default:o(()=>[e("1.2 安装扩展包和无 GUI 版本的 OpenCV")]),_:1})]),n("li",null,[s(a,{to:"#_1-3-使用其他包管理器"},{default:o(()=>[e("1.3 使用其他包管理器")]),_:1})])])]),u])}const f=p(i,[["render",v],["__file","index.html.vue"]]);export{f as default};
